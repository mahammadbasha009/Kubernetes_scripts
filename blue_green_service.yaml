apiVersion: v1
kind: Service

metadata:
  name: myyapp-service

spec:
  type: NodePort

  selector:
    app: myyapp
    version: blue

  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30080
