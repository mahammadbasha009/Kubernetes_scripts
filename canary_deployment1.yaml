apiVersion: apps/v1
kind: Deployment
metadata:
  name: myapp-v2
  namespace: canary-demo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: myapp
      version: v2
  template:
    metadata:
      labels:
        app: myapp
        version: v2
    spec:
      containers:
        - name: myapp
          image: nginxdemos/hello
          ports:
            - containerPort: 80
